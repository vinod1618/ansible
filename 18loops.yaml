- name: install packages using the loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing package
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "mysql", state: "absent" }
    - { name: "nginx", state: "present" }
    - { name: "zip", state: "absebt" }