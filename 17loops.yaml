- name: install packages using the loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing package
    ansible.builtin.package:
      name: item
      state: present
    loop:
    - nginx
    - mysql
    - zip